-- // REALISTIC MOBILE ACCEL (SMOOTH & STABLE 2026) //
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local LocalPlayer = Players.LocalPlayer

_G.AccelEnabled = false
_G.MaxSpeed = 220 -- Batas kecepatan diturunkan agar tidak nembus 500
_G.BoostPower = 0.35 -- Dorongan diperkecil agar sangat halus
_G.IsWPressed = false

-- // SETUP UI //
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
if PlayerGui:FindFirstChild("AccelGui") then PlayerGui.AccelGui:Destroy() end

local ScreenGui = Instance.new("ScreenGui", PlayerGui)
ScreenGui.Name = "AccelGui"
local Btn = Instance.new("TextButton", ScreenGui)
Btn.Size = UDim2.new(0, 150, 0, 50)
Btn.Position = UDim2.new(0.05, 0, 0.4, 0)
Btn.Text = "SOFT BOOST: OFF"
Btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Btn.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", Btn)

local function StopW()
    if _G.IsWPressed then
        VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.W, false, game)
        _G.IsWPressed = false
    end
end

Btn.Activated:Connect(function()
    _G.AccelEnabled = not _G.AccelEnabled
    Btn.Text = "SOFT BOOST: " .. (_G.AccelEnabled and "ON" or "OFF")
    Btn.BackgroundColor3 = _G.AccelEnabled and Color3.fromRGB(0, 200, 255) or Color3.fromRGB(30, 30, 30)
    if not _G.AccelEnabled then StopW() end
end)

-- // LOGIKA HANDLING HALUS //
RunService.Heartbeat:Connect(function()
    if _G.AccelEnabled then
        local char = LocalPlayer.Character
        local seat = char and char:FindFirstChildOfClass("Humanoid") and char:FindFirstChildOfClass("Humanoid").SeatPart
        
        if seat and seat:IsA("VehicleSeat") then
            local isMobileGasPressed = seat.Throttle ~= 0
            local speed = seat.AssemblyLinearVelocity.Magnitude
            
            if isMobileGasPressed then
                -- 1. Simulasi W Virtual
                if not _G.IsWPressed then
                    VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.W, false, game)
                    _G.IsWPressed = true
                end
                
                -- 2. Tambahan Kecepatan Sangat Halus
                if speed < _G.MaxSpeed then
                    -- Dorongan linear yang sangat kecil
                    seat.AssemblyLinearVelocity = seat.AssemblyLinearVelocity + (seat.CFrame.LookVector * _G.BoostPower)
                end
                
                -- 3. Downforce Ringan agar ban tidak slip
                local vel = seat.AssemblyLinearVelocity
                seat.AssemblyLinearVelocity = Vector3.new(vel.X, -5, vel.Z)
            else
                -- Langsung berhenti saat jempol lepas dari tombol gas
                StopW()
                -- Efek pengereman alami
                if speed > 1 then
                    seat.AssemblyLinearVelocity = seat.AssemblyLinearVelocity * 0.98
                end
            end
        else
            StopW()
        end
    end
end)
